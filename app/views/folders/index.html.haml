%p#notice= notice
.container
  .h1 Folders
  %table.table.table-bordered.table-striped.table-condensed
    %thead
      %tr
        %th Token
        %th Name
        %th Filesize limit
        %th Capacity
        %th Expires at
    %tbody
      - @folders.each do |folder|
        %tr
          %td= link_to folder.token, folder_path(folder.token)
          %td= folder.name
          %td
            = folder.filesize_limit
            MB
          %td
            = folder.capacity
            GB
          %td= folder.expires_at
  = link_to 'New Folder', new_folder_path, class: "btn btn-primary"
